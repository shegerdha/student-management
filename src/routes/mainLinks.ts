type Link = {
  path: string;
  label: string;
};

const links: Link[] = [
  { path: "/", label: "صفحه اصلی" },
  { path: "/about", label: "درباره ما" },
  { path: "/students-management", label: "لیست دانشجویان" },
];

export default links;
