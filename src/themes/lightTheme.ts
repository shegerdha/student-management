import type { ThemeDefinition } from "vuetify";

const lightTheme: ThemeDefinition = {
  dark: false,
  colors: {
    primary: "#7e7d7d",
    secondary: "#0003a2",
    background: "#ffffff",
    surface: "#ffffff",
    error: "#ff5252",
    success: "#4caf50",
  },
};

export default lightTheme;
