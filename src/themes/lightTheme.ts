import type { ThemeDefinition } from "vuetify";

const lightTheme: ThemeDefinition = {
  dark: false,
  colors: {
    primary: "#6200ea",
    secondary: "#03dac6",
    background: "#ffffff",
    surface: "#ffffff",
    error: "#ff5252",
    success: "#4caf50",
  },
};

export default lightTheme;
